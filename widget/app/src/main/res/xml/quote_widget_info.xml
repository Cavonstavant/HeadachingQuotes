<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is a 6-hour backstop, not the main update mechanism — the
  rollover alarm in WidgetScheduler is. The system batches these ticks and will
  not wake the device for them, and because the quote is recomputed from the
  clock on every render, any tick self-corrects a widget that missed its alarm.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_quote"
    android:previewLayout="@layout/widget_quote"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="240dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="21600000" />
